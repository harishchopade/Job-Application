services:
  - type: web
    name: job-backend
    env: java
    plan: free
    buildCommand: ./mvnw clean install
    startCommand: java -jar target/jobapplication-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
    rootDir: .
